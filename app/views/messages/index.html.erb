<%= form_for :message, url: messages_path do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
<table class="table">
    <thead>
      <tr>
        <th>Autor</th>
        <th>Text</th>
        <th>Número de tags</th>
        <th>Tags</th>
      </tr>
    </thead>
    <tbody>
      <%@messages.reverse.each do |message|%>
        <tr>
          <td><%=message.autor%></td>
          <td><%=message.text%></td>
          <td><%=message.tags.count%></td>
          <td>
              <%=message.tags.each {|t| t.name}%>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
